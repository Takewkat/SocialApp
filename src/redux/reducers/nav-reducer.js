let initialState = {
    friendsData: [
        {id: 1, name: 'Peter'},
        {id: 2, name: 'Hulk'},
        {id: 3, name: 'Thor'}
    ]
};

const navReducer = (state = initialState, action) => {
    return state;
}

export default navReducer;